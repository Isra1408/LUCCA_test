import { Nav } from "../Header/Nav";
import { Cover } from "../Header/Cover";

export const Header = () => {
  return (
    <header className="header">
      <Nav />
      <Cover />
    </header>
  );
};
